type AccountControlBtnProps = {
  text: string;
  colorScheme: string;
  color: string;
  width: string;
};

type TextBox = {
  title: string;
};

type LayoutProps = {
  readonly children: ReactElement;
};

type FormInputProps = {
  type: string
  label: string
  placeholder: string
}

type myPageTextBoxProps = {
  htmlFor: string
  placeholder:string
}